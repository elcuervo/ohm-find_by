Gem::Specification.new do |s|
  s.name              = "ohm-find_by"
  s.version           = "0.0.2"
  s.summary           = "find_by AR style to Ohm"
  s.description       = "A simple way of having a find_by implementation in Ohm"
  s.authors           = ["elcuervo"]
  s.email             = ["yo@brunoaguirre.com"]
  s.homepage          = "http://github.com/elcuervo/ohm-find_by"
  s.files = <%= Dir[
    "LICENSE",
    "README*",
    "lib/**/*.rb"
  ].inspect %>
  s.add_dependency("ohm")
end
